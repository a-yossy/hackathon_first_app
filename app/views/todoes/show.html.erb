Todo:<%= @todo.content %>

<% if !@todo.memo.blank? %>
  メモ:<%= @todo.memo %>
<% end %>

<%= link_to "編集", edit_todo_path(@todo.id) %>
<%= link_to "削除", todo_path(@todo.id), method: :delete, data: { confirm: '削除しますか？' } %>